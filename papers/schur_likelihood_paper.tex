\documentclass[11pt]{article}

\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{microtype}
\usepackage[T1]{fontenc}
\IfFileExists{newtxtext.sty}{\usepackage{newtxtext}\usepackage{newtxmath}}{\usepackage{lmodern}}
\usepackage{natbib}
\usepackage[hidelinks]{hyperref}

\newcommand{\Rhat}{\widehat{R}}
\newcommand{\tr}{\operatorname{tr}}
\newcommand{\logdet}{\log\det}

\title{\vspace{-2em}Schur Pseudo-Likelihood\\[0.3em]
       \large Scoring and Regularizing Correlation in High Dimensions}
\author{Peter Cotton}
\date{Draft --- \texttt{microprediction/precise}}

\begin{document}
\maketitle

\begin{abstract}
\noindent
We introduce the \emph{Schur pseudo-likelihood}, a one-parameter family that damps the cross-block
coupling of the Gaussian likelihood through its Schur complements, for \emph{scoring} and regularizing
covariance and correlation estimates in high dimensions. The Gaussian log-likelihood is the standard
criterion, but when the dimension rivals the sample size it is governed by the smallest,
least-identifiable eigenvalues of the estimate, and as a criterion for ranking or selecting estimates
it becomes unreliable; damping the coupling restores it. The optimal damping has a closed
form---the reliability of the coupling, a James--Stein shrinkage. On real crypto returns, choosing a
shrinkage estimate by the Schur pseudo-likelihood rather than by the full likelihood yields several-fold
lower out-of-sample portfolio variance once the dimension rivals the sample size.
\end{abstract}

\section{The high-dimensional failure of the Gaussian likelihood}
A correlation matrix is the hard part of a covariance matrix: variances are a one-dimensional,
per-series problem, while the high-dimensional difficulty---the near-singular spectrum and the unstable
inverse---lives in the correlation. The standard way to judge a correlation (or covariance) estimate
$\Rhat$ is the log-likelihood it assigns to held-out data,
$\ell(\Rhat) = -\tfrac12(\logdet\Rhat + \tr(\Rhat^{-1}S))$ for a test covariance $S$. Both terms are
governed by the smallest eigenvalues of $\Rhat$: $\logdet\Rhat=\sum_i\log\lambda_i$ diverges as any
$\lambda_i\to0$, and $\Rhat^{-1}$ diverges with it. When $p$ rivals $n$ those eigenvalues are
unidentifiable (the Mar\v{c}enko--Pastur regime), so the likelihood is dominated by the least reliable
part of the spectrum \citep{ledoit2004,bun2017}. As a \emph{criterion}, it then fails: in a controlled
experiment in which the truth is known, the held-out likelihood ranks the better of two estimates below
chance, whereas inversion-free criteria such as Frobenius distance and the variogram score
\citep{scheuerer2015} exceed $80\%$. This is the evaluation-side analogue of the instability of the
minimum-variance portfolio $w\propto\Rhat^{-1}\mathbf1$, which fails through the same inverse. The
remedy is to discount that inverse---continuously, through one interpretable parameter, rather than by
discarding the cross-block structure outright.

\section{The Schur pseudo-likelihood}
Partition the variables into $K$ ordered blocks. The Gaussian density factors exactly into block
conditionals, $p(x)=\prod_k p(x_k\mid x_{<k})$, each Gaussian with covariance the Schur complement
\[
  S_k \;=\; R_{kk} - R_{k,<k}\,R_{<k,<k}^{-1}\,R_{<k,k},
  \qquad
  \mu_{k\mid<k} = R_{k,<k}R_{<k,<k}^{-1}x_{<k},
\]
so $\logdet R=\sum_k\logdet S_k$ and the likelihood is a sum of block-conditional terms, each inverting
only the conditioning block \citep{vecchia1988,katzfuss2021}. We damp the coupling by $\gamma\in[0,1]$,
\[
  S_k(\gamma) = (1-\gamma)\,R_{kk} + \gamma\,S_k,
  \qquad
  \mu_{k\mid<k}(\gamma) = \gamma\,R_{k,<k}R_{<k,<k}^{-1}x_{<k},
\]
and score $\ell_\gamma(R;x)=\sum_k\log\mathcal N(x_k;\mu_{k\mid<k}(\gamma),S_k(\gamma))$. At $\gamma=1$
this is the exact joint likelihood; at $\gamma=0$ it is the block-diagonal composite likelihood
\citep{besag1975,lindsay1988,varin2011}; intermediate $\gamma$ inverts no block larger than a
conditioning set and is better conditioned. The same $\gamma$ interpolates an allocation rule, from
hierarchical risk parity at $\gamma=0$ \citep{lopezdeprado2016} to minimum variance at $\gamma=1$
\citep{cotton2024}.

\section{The optimal damping is the coupling reliability}
In a tractable two-block model---$x_2=bx_1+\varepsilon$ with conditional $R^2$ equal to
$\rho^2$---minimising the expected predictive residual of the damped score gives a closed form,
\[
  \gamma^\star \;=\; \frac{b^2}{b^2+\operatorname{Var}(\hat b)}
            \;=\; \frac{(n-2)\,\rho^2}{(n-2)\,\rho^2+(1-\rho^2)},
\]
the \emph{reliability} of the estimated coupling: a Wiener/James--Stein shrinkage
\citep{james1961,ledoit2012}. It tends to $1$ as $n\to\infty$ or $\rho^2\to1$ and to $0$ as
$\rho^2\to0$, and is interior when the coupling is partially reliable. The empirical optimum tracks it:
sweeping a synthetic block correlation's coupling strength $v$ against the aspect ratio $n/p$
(Figure~\ref{fig:gamma}), the $\gamma$ minimising recovery error rises monotonically with $v$, from
$0$ (no coupling) to $1$ (strong coupling), and dips where samples are scarcest---the reliability law,
recovered across the plane.

\begin{figure}[h]\centering
  \includegraphics[width=0.84\linewidth]{figures/corr_gamma_star.png}
  \caption{Optimal Schur $\gamma^\star$ over coupling strength $v$ and aspect ratio $n/p$ ($p=128$,
  synthetic). It rises monotonically with $v$---the reliability law---and dips where samples are
  scarcest.}\label{fig:gamma}
\end{figure}

\section{Selecting estimates in high dimensions: real data}
The decisive test of a \emph{scoring rule} is whether the estimate it prefers generalizes. We test it
on real daily crypto returns ($72$ liquid assets) with a same-frequency, disjoint-split design that
avoids both look-ahead and the cross-frequency attenuation (Epps) that confounds naive
hourly-versus-daily comparisons: within a window, days are partitioned at random into a training set
(an estimate is built), a validation set (scoring rules choose among a shrinkage ladder
$R_\delta=(1-\delta)R_{\text{emp}}+\delta I$), and a held-out test set. The external criterion is the
realized variance of the minimum-variance portfolio formed from the selected estimate---neither scoring
rule, so the comparison is not circular.

We compare every scoring rule that is usable out of sample: the full likelihood ($\gamma=1$), the Schur
pseudo-likelihood, the block/composite likelihood ($\gamma=0$), Stein loss, the variogram score, and---as
a matched, non-circular benchmark, since validation and test are disjoint---the validation GMV variance
itself. A clear split emerges (Figure~\ref{fig:select}). When $n/p\le1$ the \emph{inverse-heavy} scores
select too little shrinkage and build fragile portfolios: the full likelihood and Stein loss give
median out-of-sample variance roughly six times higher than the rest. The \emph{inverse-discounting}
scores---the Schur pseudo-likelihood, the composite likelihood, and the matched GMV criterion---select
near-optimally; Schur matches the composite likelihood (window-clustered bootstrap difference within
$\pm0.01$) and comes within a few percent of the matched benchmark, while beating the full likelihood,
Stein, and variogram with $95\%$ CIs excluding parity. All converge once $n/p>1.5$, where the full
likelihood becomes reliable. That Schur \emph{ties} the composite endpoint here is the reliability law
at work: crypto returns are dense and factor-driven, so for arbitrary blocks the coupling is barely
reliable, $\gamma^\star$ is small, and the dial lands near $\gamma=0$. The interior-$\gamma$ advantage
of Schur over the composite endpoint appears when the coupling is partially reliable---the synthetic
regime of the previous section. The robust message is that discounting the inverse is what matters in
high dimensions, and the Schur dial is the principled, closed-form way to do it, spanning composite and
full likelihood with $\gamma^\star=$ reliability.

\begin{figure}[h]\centering
  \includegraphics[width=0.78\linewidth]{figures/scoring_selection_crypto.png}
  \caption{Real crypto. Out-of-sample minimum-variance-portfolio variance (median, log scale) of the
  estimate \emph{selected} by each scoring rule, versus aspect ratio. Selecting by the Schur
  pseudo-likelihood is several-fold safer than by the full likelihood when undersampled; they converge
  once well sampled.}\label{fig:select}
\end{figure}

\section{The same selector across domains}
The minimum-variance portfolio is one instance of $w\propto\Sigma^{-1}\mathbf1$; forecast combination
\citep{markowitz1952}, Gaussian-process regression, and Kalman filtering are others, and all are tuned
by the same log-det-plus-quadratic likelihood---indeed scalable GPs already use the block-conditional
(Vecchia) factorization \citep{vecchia1988,katzfuss2021} on which $\ell_\gamma$ is built. We therefore
run the \emph{identical} selection experiment on residual panels from five domains: daily crypto
returns; real numerical-weather forecast residuals (archived forecast minus ERA5 reanalysis) on a
$323$-point CONUS grid; draws from a known block correlation and from a spatial Gaussian-process
kernel; and the innovations of a stable vector autoregression (Kalman-style residuals). In every
domain, when $n/p\le1$, selecting by the full likelihood gives higher out-of-sample combined-residual
variance than selecting by the Schur pseudo-likelihood (Table~\ref{tab:domains},
Figure~\ref{fig:domains}); the inverse-discounting scores cluster near the safe optimum. The gap is
largest for the Gaussian-process field, whose smooth kernel is the most ill-conditioned, and mildest
for the weather forecast residuals, which are noisier and less correlated than the underlying field.
The pathology and its remedy are thus not financial: wherever a high-dimensional covariance is inverted
to combine correlated signals, the full likelihood is a fragile selection criterion and a damped one is
not.

\begin{table}[h]\centering\small
\caption{Ratio of out-of-sample combined-residual variance, full-likelihood selection over
Schur-pseudo-likelihood selection, undersampled regime ($n/p\le1$). Greater than one means Schur
selection is safer.}\label{tab:domains}
\begin{tabular}{lc}
\toprule
domain & full-likelihood $/$ Schur \\
\midrule
Gaussian-process field            & 18.1\\
VAR innovations (Kalman-style)    & 4.0\\
crypto (portfolio)                & 2.7\\
synthetic block correlation       & 2.7\\
weather forecast residuals (CONUS)& 1.8\\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[h]\centering
  \includegraphics[width=\linewidth]{figures/generalize_domains.png}
  \caption{The same selection experiment across five domains: out-of-sample combined-residual variance
  (median, log) of the estimate selected by each scoring rule, versus aspect ratio. The full
  likelihood is fragile when undersampled in every domain; inverse-discounting scores are not.}
  \label{fig:domains}
\end{figure}

\section{Regularization and conditioning}
The damping is a structured shrinkage: $S_k(\gamma)=(1-\gamma)R_{kk}+\gamma S_k$, a convex combination
of positive-definite matrices, pulls each conditional covariance toward its unconditional block. This
raises the smallest eigenvalues and lowers the condition number relative to the full likelihood, and
yields a usable inverse even when the empirical correlation is singular ($n<p$); the block-diagonal
endpoint is positive definite whenever the blocks are. Where the linear interpolation can itself
approach singularity---near a vanishing Schur complement under strong coupling---the affine-invariant
SPD-geodesic interpolation lifts the small eigenvalues multiplicatively and stays well conditioned by
construction. This conditioning is the method's general high-dimensional benefit: the same $\gamma$
that maximises the pseudo-likelihood regularizes any downstream use of the inverse, the
evaluation-side analogue of eigenvalue shrinkage \citep{ledoit2004,ledoit2012,bun2017}.

\section{Scope: a scoring and regularization device, not a free estimator}
The maximiser of $\ell_\gamma$ recovers the true predictive law for every $\gamma$ while implying an
inflated correlation, so $\ell_\gamma$ is a scoring and regularization device, not an estimation
objective. Used \emph{as} an estimator the same damping helps recovery only in its own regime---a
genuinely block-structured correlation with partially-reliable coupling (synthetic), where interior
$\gamma$ beats both endpoints---and not on dense, strongly factor-driven data such as crypto, where the
empirical correlation recovers best and arbitrary block damping discards real signal. The optimal
damping also inverts with the target: recovering the matrix wants large $\gamma$, recovering its inverse
(partial correlations) wants small $\gamma$, because the inverse amplifies coupling noise. ``Best''
is undefined until the target is named; the contribution here is a principled, closed-form dial that is
right for scoring and regularizing the inverse-fragile, high-dimensional case.

\section{Conclusion}
The Schur pseudo-likelihood is a one-knob interpolation between decoupled and full Gaussian scoring
whose optimal setting is the closed-form reliability of the cross-block coupling. In high dimensions,
where the Gaussian likelihood is dominated by unidentifiable small eigenvalues and fails as a criterion,
it is a better-behaved, better-conditioned score: selecting estimates by it yields markedly safer
out-of-sample portfolios on real data when the dimension rivals the sample size.

\bibliographystyle{plainnat}
\bibliography{refs}

\end{document}
