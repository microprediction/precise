\documentclass[11pt]{article}

\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{microtype}
\usepackage[T1]{fontenc}
% Times-like text and math; falls back gracefully if newtx is unavailable.
\IfFileExists{newtxtext.sty}{\usepackage{newtxtext}\usepackage{newtxmath}}{\usepackage{lmodern}}
\usepackage{natbib}
\usepackage[hidelinks]{hyperref}

\newcommand{\Shat}{\widehat{\Sigma}}
\newcommand{\tr}{\operatorname{tr}}
\newcommand{\logdet}{\log\det}
\theoremstyle{plain}\newtheorem{proposition}{Proposition}

\title{\vspace{-2em}Schur Pseudo-Likelihood\\[0.3em]
       \large A Principled Way to Evaluate Covariance in High Dimensions}
\author{Peter Cotton}
\date{Draft --- \texttt{microprediction/precise}}

\begin{document}
\maketitle

\begin{abstract}
\noindent
The Gaussian log-likelihood is the standard criterion for scoring a covariance estimate, but in high
dimensions it is governed by the smallest eigenvalues of the estimate---precisely the part of the
spectrum that is unidentifiable when the dimension is comparable to the sample size---and we show that
it ranks estimators below chance. We introduce the \emph{Schur pseudo-likelihood}, a one-parameter
family $\ell_\gamma$, $\gamma\in[0,1]$, obtained by damping the cross-block coupling of the Gaussian
likelihood through its Schur complements. At $\gamma=1$ the family is the exact joint likelihood; at
$\gamma=0$ it is the block-diagonal (composite) likelihood; intermediate $\gamma$ yields a
better-conditioned interpolant. The construction is the evaluation-side counterpart of Schur
complementary portfolio allocation, which interpolates hierarchical risk parity and the
minimum-variance portfolio through the same Schur-complement control. In a tractable two-block model we
obtain the optimal coupling strength in closed form---it is the \emph{reliability} of the coupling, a
Wiener/James--Stein shrinkage---and show that the maximizer of $\ell_\gamma$ recovers the true
predictive law for every $\gamma$ while implying an inflated covariance, which clarifies why
$\ell_\gamma$ is a scoring and regularization device rather than a free estimation objective. We
characterize when an interior $\gamma$ improves discrimination, locate the high-dimensional advantage
in the variance of the estimate rather than the test sample, and state precisely what the present
analysis does and does not establish.
\end{abstract}

\section{The high-dimensional failure of the Gaussian likelihood}

Covariance and precision matrices are the inputs to portfolio construction \citep{markowitz1952},
risk measurement, Gaussian and graphical models, and linear discriminant analysis, and the quality of
those downstream decisions is bounded by the quality of the estimate. The estimate is never observed
against ground truth, so it must be judged by its performance on held-out data; how to do so reliably,
and in particular in high dimensions, is the question of this paper.

The motivation is not only diagnostic. A cheap and reliable out-of-sample score is what makes online
adaptation feasible: in a streaming setting one can compare candidate estimators on recent held-out
data and switch, blend, or retune them as the regime changes, rather than committing to a single
estimator chosen offline. Because no estimator is uniformly best across dimension, spectrum, and tail
behavior \citep{ledoit2004}, this selection is unavoidable, and it is only as good as the score that
drives it. The choice of score is, moreover, known to matter for ranking whenever the target is
observed only through a noisy proxy \citep{patton2011, laurent2013}; we show below that in high
dimensions the standard score fails in precisely the regime where selection matters most.

The conventional criterion for the quality of a covariance estimate is the likelihood it assigns to
held-out data. For a zero-mean Gaussian,
\[
  \ell(\Shat;x) \;=\; -\tfrac12\bigl(\logdet\Shat + x^\top \Shat^{-1} x\bigr),
\]
or, in terms of the sample covariance $S$ of a test set,
$\ell(\Shat;S) = -\tfrac12\bigl(\logdet\Shat + \tr(\Shat^{-1}S)\bigr)$, which is proportional to the
Wishart log-likelihood and, up to constants, to the negative Stein loss \citep{james1961}. The
criterion is a strictly proper scoring rule \citep{gneiting2007} and is the objective that maximum
likelihood optimizes; in low dimensions it is the appropriate choice.

Both terms are, however, controlled by the smallest eigenvalues of $\Shat$: the log-determinant is
$\sum_i \log\lambda_i$, which diverges to $-\infty$ as any $\lambda_i\to 0$, and $\Shat^{-1}$ diverges
in the same limit. When the dimension $p$ is comparable to the sample size $n$, these eigenvalues
concentrate near zero and become statistically unidentifiable (the Mar\v{c}enko--Pastur regime), so
the likelihood is determined largely by the least reliable part of the spectrum. Empirically the
effect is pronounced. In a controlled experiment in which the correct ordering of two estimates is
known---the true Kullback--Leibler divergence is available because the population covariance is
specified---the held-out likelihood selects the better estimate in approximately $45\%$ of trials,
below the chance rate, whereas inversion-free criteria such as the Frobenius distance and the
variogram score \citep{scheuerer2015} attain approximately $85\%$. This is the evaluation-side analogue of the instability
of the minimum-variance portfolio $w \propto \Shat^{-1}\mathbf{1}$, which fails through the same
$\Shat^{-1}$ and the same eigenvalues. In both settings the remedy is to discount the raw inverse; the
contribution of this paper is to do so continuously, through a single interpretable parameter, rather
than by discarding the cross-block structure outright.

\section{Exact Schur factorization}

Partition the variables into $K$ ordered blocks, $x=(x_1,\dots,x_K)$. The joint density factors
exactly into a product of block conditionals, $p(x)=\prod_k p(x_k\mid x_{<k})$, and for a Gaussian each
conditional is Gaussian with covariance the Schur complement
\[
  S_k \;=\; \Sigma_{kk} - \Sigma_{k,<k}\,\Sigma_{<k,<k}^{-1}\,\Sigma_{<k,k}
\]
and mean $\mu_{k\mid<k} = \Sigma_{k,<k}\Sigma_{<k,<k}^{-1}x_{<k}$. Consequently
$\logdet\Sigma = \sum_k \logdet S_k$, the quadratic form decomposes blockwise, and the likelihood is a
sum of block-conditional terms, each of which inverts only the conditioning block $\Sigma_{<k,<k}$.
This is the block Cholesky factorization, and it underlies the Vecchia approximation
\citep{vecchia1988, katzfuss2021, pan2024}, Gaussian--Markov-random-field likelihoods
\citep{rue2005}, and nested-dissection elimination \citep{george1973}. No approximation has been
introduced; the likelihood has merely been rewritten in a form amenable to a single modification.

\section{The Schur pseudo-likelihood}

We damp the Schur complement, and the associated conditional regression, by a parameter
$\gamma\in[0,1]$:
\[
  S_k(\gamma) \;=\; \Sigma_{kk} - \gamma\,\Sigma_{k,<k}\Sigma_{<k,<k}^{-1}\Sigma_{<k,k}
              \;=\; (1-\gamma)\,\Sigma_{kk} + \gamma\,S_k,
  \qquad
  \mu_{k\mid<k}(\gamma) = \gamma\,\Sigma_{k,<k}\Sigma_{<k,<k}^{-1}x_{<k},
\]
and define the score
$\ell_\gamma(\Sigma;x) = \sum_k \log \mathcal{N}\!\bigl(x_k;\,\mu_{k\mid<k}(\gamma),\,S_k(\gamma)\bigr)$.
At $\gamma=1$ this is the exact joint likelihood. At $\gamma=0$ each block is scored under its
unconditional covariance $\Sigma_{kk}$ with no reference to the others, which is the block-marginal
composite likelihood \citep{besag1975, lindsay1988, varin2011}. For
$\gamma\in(0,1)$ the family modifies the block covariances rather than the data, inverts no matrix
larger than a conditioning block, and is better conditioned than the full likelihood because damping
raises the small eigenvalues. We refer to $\ell_\gamma$ as the Schur pseudo-likelihood.

The nature of the object requires care, as it bears on the analysis below. At $\gamma=1$, $\ell_\gamma$
is a normalized joint density; at $\gamma=0$ it is a composite likelihood, a sum of valid component
log-densities that does not integrate to one. For $\gamma\in(0,1)$ each term remains a proper Gaussian
log-density on its block, but the damped conditionals are mutually inconsistent---they are not the
conditionals of any single joint Gaussian---so $\ell_\gamma$ is a pseudo-likelihood. As shown in the
next section, $\ell_\gamma$ is therefore appropriate for scoring a covariance, or for damping a given
one, but not for free maximization over $\Sigma$.

\section{Behavior in a tractable model}

The action of $\gamma$ can be characterized exactly in the smallest informative case. Let
$x_1\sim\mathcal{N}(0,a)$ and $x_2 = b\,x_1 + \varepsilon$ with $\varepsilon\sim\mathcal{N}(0,s)$, so
that the true conditional law of $x_2$ given $x_1$ has slope $b$ and conditional variance $s$.

\begin{proposition}[Two-block model]
For every $\gamma\in(0,1]$ the maximizer of the population $\ell_\gamma$ recovers the true predictive
law---the effective slope equals $b$ and the damped conditional variance equals $s$---while the implied
covariance is inflated, with off-diagonal $\widehat{\Sigma}_{12}=\Sigma_{12}^\star/\gamma$. The implied
covariance is positive definite if and only if $\gamma^2(1-\rho^2) > (1-\gamma)\rho^2$, where
$\rho^2 = b^2 a/(b^2 a + s)$ is the conditional $R^2$; below this threshold the free maximizer leaves
the positive-definite cone.
\end{proposition}

The inflation is the key qualitative fact: a free fit compensates for the damping by scaling the
coupling by $1/\gamma$, and for sufficiently strong coupling this scaling produces a non-positive
matrix. This is the precise sense in which $\ell_\gamma$ fails as a free estimation objective for
$\gamma<1$, and it is consistent with use as a score or as a fixed transform, both of which hold
$\Shat$ fixed.

The quantity that $\gamma$ controls---the degree of trust placed in the estimated coupling---admits a
well-defined optimum. Estimating the slope from $n$ observations by least squares gives an unbiased
$\hat b$ with variance $s/(a(n-2))$. Minimizing the expected predictive residual over $\gamma$ yields
\[
  \gamma^\star \;=\; \frac{b^2}{\,b^2 + \operatorname{Var}\hat b\,}
        \;=\; \frac{(n-2)\,\rho^2}{(n-2)\,\rho^2 + (1-\rho^2)}.
\]
Thus $\gamma^\star$ is the reliability of the coupling, a Wiener/James--Stein shrinkage: it increases
toward $1$ as the sample size or coupling strength grows and decreases toward $0$ as the coupling
becomes uninformative, and is interior when the coupling is partially reliable. The closed form agrees
with the simulated optimum. In the vector case $\rho^2$ is replaced by the largest squared canonical
correlation between the blocks; across $K$ blocks the inflation, and the positive-definiteness
threshold, compound along the conditioning chain.

\section{Discrimination efficiency and the Godambe boundary}

It is tempting to credit the interior optimum to the variance of the held-out score, but that
attribution is mistaken, and separating the two sources of variability is what makes the mechanism
clear. Write the score as a quadratic form,
$\ell_\gamma(\Shat;x) = -\tfrac12\bigl(\mathrm{Ld}_\gamma + x^\top W_\gamma(\Shat)\,x\bigr)$, where
$W_\gamma$ assembles the damped block conditionals and $W_1=\Shat^{-1}$. For two fixed estimates and a
Gaussian test sample, the probability of ranking the better estimate above the worse is governed by a
per-sample signal-to-noise ratio, which is maximized at $\gamma=1$; under test-sample noise alone the
full likelihood is optimal, and damping introduces only bias. The high-dimensional advantage of an
interior $\gamma$ is therefore not attributable to test-sample variance.

The advantage arises from the variability of the estimate. The score depends on $\Shat$ through
$W_\gamma$, and at $\gamma=1$ the sensitivity of $W_1=\Shat^{-1}$ scales as $1/\lambda_{\min}^2$; the
variance of the score difference across training draws then scales as $1/\lambda_{\min}^4$, which grows
sharply as the population becomes ill-conditioned, whereas a damped $\gamma$ keeps it bounded. When the
discriminating signal resides in the within-block structure and the cross-block coupling is shared
noise, an interior $\gamma$ strictly improves on both endpoints (a ranking accuracy of approximately
$0.84$ against $0.45$ at the extremes). The parameter is thus a bias--variance control over the
estimate rather than over the test point.

Finally, the estimating equation $\nabla_\Sigma\ell_\gamma$ is unbiased at the truth only at the
endpoints---$\gamma=1$ (the Fisher score) and $\gamma=0$ (correctly specified block marginals)---and
is biased in the interior, reflecting the inflation of the previous section. Classical Godambe
efficiency therefore applies only at the endpoints, where $\gamma=0$ carries the standard
composite-likelihood efficiency loss relative to the full likelihood; the interior is a tempering,
justified by discrimination rather than by consistency. This formalizes the distinction between scoring
with $\ell_\gamma$ and estimating with it.

\section{Damping as structured shrinkage}

The transform interpretation---damping the coupling of a given covariance---is a shrinkage. Each
conditional covariance is shrunk from its full Schur complement toward the unconditional block,
$S_k(\gamma)=(1-\gamma)\Sigma_{kk}+\gamma S_k$, with intensity $1-\gamma$. Since conditioning reduces
variance, $S_k\preceq\Sigma_{kk}$, this raises the conditional covariance and lifts the small
eigenvalues of every block-sized inverse the score performs; it is a James--Stein shrinkage applied
blockwise to the conditioning. The operation is complementary to spectral shrinkage: Ledoit--Wolf and
rotation-invariant estimators regularize the eigenvalues (basis-free), whereas Schur damping
regularizes the cross-block coupling (basis-dependent). The two compose, and $\gamma$ is to the block
factorization what the shrinkage intensity \citep{ledoit2004, ledoit2012, bun2017} is to the spectrum.

\section{A unifying view}

The same control organizes several constructions onto two axes of the block factorization. The first
axis is the conditioning structure---which earlier blocks each block conditions on---which the Vecchia
approximation, Gaussian--Markov random fields, banding, and nested dissection regularize through
sparsity at full coupling strength. The second axis is the coupling strength $\gamma$, which those
methods do not vary. The axes are orthogonal and compose.

\begin{center}\small
\begin{tabular}{lll}
\hline
 & $\gamma=1$ (full trust) & $\gamma=0$ (no trust)\\
\hline
dense conditioning & exact Gaussian likelihood; MVO $w\propto\Sigma^{-1}\mathbf 1$ & block composite likelihood; HRP\\
sparse conditioning & block-Vecchia / GMRF likelihood & (block-marginal, same column)\\
\hline
\multicolumn{3}{l}{interior $\gamma$: the Schur pseudo-likelihood, the interpolant absent from the above}\\
\hline
\end{tabular}
\end{center}

\noindent
The portfolio entries are not analogical. Minimum-variance optimization is the dense-conditioning,
$\gamma=1$ corner; hierarchical risk parity \citep{lopezdeprado2016} is the $\gamma=0$ corner; Schur
complementary allocation \citep{cotton2024} interpolates them by damping the same Schur complement
through the same $\gamma$. The Schur pseudo-likelihood is that construction applied to evaluation, and
the reason an interior $\gamma$ improves on the minimum-variance endpoint is the reason hierarchical
risk parity is more stable than unconstrained mean--variance optimization \citep{antonov2024}.

\section{Generality}

The construction requires only a Gaussian (or Gaussian-pseudo) block likelihood and a partition, so
the coupling-strength control applies to Gaussian-block models generally. In spatial statistics and
Gaussian-process regression, where Vecchia and GMRF likelihoods condition each location on a
neighborhood, $\gamma$ supplies a robustness control on the strength of that conditioning, orthogonal
to the choice of neighborhood. In Gaussian graphical models it tempers conditional-dependence strength
between the full penalized likelihood and node-wise pseudo-likelihood. In longitudinal and state-space
models, with time slices as blocks, it interpolates between independent slices and the full temporal
joint. Wherever composite likelihood is used for tractability, $\gamma$ provides a continuous path back
toward the full likelihood. These are statements about the shared mechanism rather than empirical
results; the covariance-evaluation case is the one studied directly here.

\section{Interpolation geometry and empirical results}

Damping interpolates each conditional covariance between $\Sigma_{kk}$ and $S_k$ along a straight line.
An alternative is the affine-invariant SPD geodesic,
$\Sigma_{\mathrm{BD}}\,\#_\gamma\,\Sigma = \Sigma_{\mathrm{BD}}^{1/2}(\Sigma_{\mathrm{BD}}^{-1/2}
\Sigma\,\Sigma_{\mathrm{BD}}^{-1/2})^{\gamma}\Sigma_{\mathrm{BD}}^{1/2}$, which lifts small eigenvalues
multiplicatively. The two interpolations agree at the endpoints and to first order and differ near a
singular Schur complement; in that strong-coupling regime the linear interpolation exhibits a loss of
discriminating power that the geodesic interpolation does not.

The empirical findings are threefold. The optimal $\gamma$ is regime-dependent: it approaches $1$ when
the coupling is reliable, approaches $0$ when it is noise, and is strictly interior---improving on both
endpoints---when the coupling is partially reliable, consistent with the bias--variance account above.
The geodesic variant is the more stable default where the two interpolations diverge. Finally, on real
equity returns (Ken French portfolios, rolling minimum-variance out-of-sample variance), estimators
that invert an unregularized covariance degrade by orders of magnitude while well-conditioned shrinkage
does not; this is not a test of $\ell_\gamma$ as an equity estimator, but it corroborates the central
mechanism, namely that high-dimensional performance is governed by the conditioning of the inverse.


% === precise-lab empirical addition ===
% ====================================================================
% DRAFT empirical addition for schur_likelihood_paper.tex
% Insert inside / after \section{Interpolation geometry and empirical results}.
% Numbers tagged [prelim] refresh when the full 28,800-cell grid completes.
% Figures referenced live in papers/figures/ (copied from precise-lab/results/figures).
% ====================================================================

\subsection{A controlled estimator/evaluator grid}
We sweep a $144$-combination grid: four ground-truth generators (\textsc{lkj}, \textsc{wishart},
\textsc{residuals}, \textsc{walk}; the last two strongly spiked and near-singular respectively),
dimension $p\in\{8,16,32,64,128,256\}$, and aspect ratio $n/p\in\{\tfrac12,1,2,4,8,16\}$, each at
$200$ replications. Every one of $38$ online estimator configurations is streamed one observation at
a time through \texttt{partial\_fit} and scored by all shipped assessors plus the exact gold
$\mathrm{KL}(\Sigma\,\|\,\Shat)$. The setup is reproducible from \texttt{precise-lab}.

\paragraph{The interpolant occupies the regime the theory predicts.}
Ranking estimators by realized minimum-variance-portfolio variance, the winner is regime-dependent
exactly as $\gamma^\star$ predicts (Fig.~\ref{fig:regime}). For $n/p\gtrsim 4$ the raw empirical
covariance wins at every $p$; for $n/p\le 1$ the winner is a shrinkage or Schur estimator, and
\emph{$\Shat^{\mathrm{Schur}}_{\gamma=1/2}$ itself wins the moderate-to-high-dimension, low-sample
cells} (e.g.\ $p\in\{64,128\}$ at $n/p\le1$) [prelim]. The crossover boundary tilts upward with $p$:
more directions to estimate require relatively more samples before the full covariance is trustworthy.

\subsection{The optimal coupling trust is shared by evaluation and allocation}
The tractable two-block model gives the closed form
$\gamma^\star=(n-2)\rho^2/[(n-2)\rho^2+(1-\rho^2)]$, the reliability of the estimated coupling. The
same quantity is optimal for the \emph{portfolio}: damping the cross-block entry of the sample
covariance by $\gamma$ before forming $w\propto \Shat(\gamma)^{-1}\mathbf 1$ interpolates the
inverse-variance/HRP allocation ($\gamma=0$) and the sample minimum-variance portfolio ($\gamma=1$),
and the $\gamma$ minimizing realized variance tracks $\gamma^\star$:

\begin{center}\small
\begin{tabular}{rccc}
\toprule
$n$ & reliability $\gamma^\star$ & $\arg\min_\gamma$ residual (eval) & $\arg\min_\gamma$ GMV var (alloc)\\
\midrule
8   & 0.72 & 0.70 & 0.00 \\
32  & 0.93 & 0.92 & 0.46 \\
128 & 0.98 & 0.98 & 0.86 \\
256 & 0.99 & 1.00 & 0.92 \\
\bottomrule
\end{tabular}
\end{center}

\noindent Both optima rise from the HRP corner toward the full covariance as the coupling becomes
reliable; $\mathrm{corr}(\arg\min\text{eval},\gamma^\star)=0.998$,
$\mathrm{corr}(\arg\min\text{alloc},\gamma^\star)\approx0.90$--$0.97$. The single asymmetry is that
\emph{allocation is the more conservative of the two}: because the portfolio passes the coupling
through $\Shat^{-1}$, it amplifies the coupling's estimation noise more than the predictive residual
does, so $\gamma^\star_{\text{alloc}}\le\gamma^\star_{\text{eval}}$ and at small $n$ the portfolio
retreats fully to HRP while the likelihood still trusts a partial coupling. This identifies HRP as the
reliability-$\to 0$ corner of the Schur family, and the minimum-variance portfolio as the
reliability-$\to1$ corner, with the data-determined optimum in between.

\paragraph{The relationship survives the hierarchy.}
On a random asymmetric nested factor truth (depth $1$--$3$, $p=8$--$32$), with a single $\gamma$
damping all cross-leaf-block entries, the evaluation-optimal and allocation-optimal $\gamma$ both
climb together with sample size at every depth, and the gap between them stays small and bounded
($\lesssim0.2$, mean $\approx0.10,0.18,0.01$ at depths $1,2,3$) with no monotone growth: the
inverse-amplification asymmetry does \emph{not} compound down the tree. A single coupling-trust
$\gamma$ (or the closed-form reliability) is therefore an adequate approximation across levels;
per-level tuning is a refinement, not a necessity. [prelim; clean per-node recursive version pending]

\subsection{Schur as the robust judge}
Treating each assessor as a model-selection test, we measure its \emph{power}: the probability that,
from a single finite test sample of size $n_{\text{test}}$, it orders a random pair of candidate
estimates the way the truth does ($0.5$ = chance), against three notions of truth. The best judge is
the one aligned with the chosen gold (Table~\ref{tab:power}, $c=p/n\approx1$, $n_{\text{test}}=2p$
[prelim]): held-out log-likelihood for the KL/density gold, the GMV score for the allocation gold, and
\emph{the Schur pseudo-likelihood for matrix recovery}. The log-likelihood is near-optimal only for
its own (KL) gold and sheds power toward chance on the practical golds, exactly the high-dimensional
failure that opens this paper; the Schur judge is never the worst and best recovers top-spectrum
structure---the evaluation-side mirror of $\gamma$ regularizing allocation.

\begin{table}[h]\centering\small
\caption{Judge power $P(\text{order pair like truth})$ by gold criterion. $0.5=$ chance.}
\label{tab:power}
\begin{tabular}{lccc}
\toprule
judge & gold $=$ KL & gold $=$ Frobenius & gold $=$ GMV var\\
\midrule
LogLikelihood     & \textbf{0.985} & 0.700 & 0.697\\
SteinLoss         & \textbf{0.985} & 0.700 & 0.698\\
SchurLikelihood   & 0.740 & \textbf{0.771} & 0.712\\
GMVVariance       & 0.693 & 0.691 & \textbf{0.949}\\
VariogramScore    & 0.598 & 0.730 & 0.575\\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[h]\centering
  \includegraphics[width=0.8\linewidth]{figures/regime_GMVVariance.png}
  \caption{Winning estimator by $(p,\,n/p)$ for realized minimum-variance-portfolio variance.
  Empirical wins when well-sampled; shrinkage and $\Shat^{\mathrm{Schur}}_{\gamma=1/2}$ win when
  undersampled. [prelim]}\label{fig:regime}
\end{figure}
\begin{figure}[h]\centering
  \includegraphics[width=\linewidth]{figures/judge_power_by_gold.png}
  \caption{Statistical power of each assessor vs concentration $c=p/n$, by gold criterion
  ($n_{\text{test}}=2p$); $0.5$ is chance. [prelim]}
\end{figure}

% === end precise-lab addition ===

\section{Limitations}

The closed-form $\gamma^\star$ is established for the predictive (estimator) use in the two-block
model. The high-dimensional discrimination optimum is only partially resolved: the variance term is
first-order and is given above ($1/\lambda_{\min}^4$ at $\gamma=1$, bounded below it), but the signal
term is second-order in the sampling noise, so a closed form for the discrimination $\gamma^\star$
requires a higher-order analysis and is at present available only numerically. The compounded
$K$-block positive-definiteness threshold is established as a two-block formula together with numerical
evidence of compounding, not yet as a closed-form chain. The dependence on block ordering, as in
Vecchia approximations, is not addressed here. The central claim is falsifiable: the interior optimum
should disappear when the coupling is either fully reliable or pure noise, and an interior optimum in a
regime known to be fully reliable would contradict it.

\section*{Acknowledgements}

The original Schur complementary theory, of which this is the evaluation-side counterpart, was
developed with the support of Intech Investments.

\bibliographystyle{plainnat}
\bibliography{refs}

\end{document}
